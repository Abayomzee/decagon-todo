import React from "react";
import { ReactComponent as ScheduleIcon } from "./svgs/scheduleIcon.svg";

interface Props {}
const schedule: React.FC<Props> = () => {
  return <ScheduleIcon />;
};

export default schedule;
